---
import Layout from '../layouts/Layout.astro';
import { SignIn } from '@clerk/astro/components';
---

<Layout title="Login - Sistema Barbearia">
	<main>
		<div class="login-background">
			<div class="login-container">
				<div class="header">
					<div class="logo-icon">💈</div>
					<h1>Sistema Barbearia</h1>
					<p>Acesse sua conta e gerencie seus agendamentos</p>
				</div>
				
				<div class="clerk-wrapper">
					<SignIn 
						appearance={{
							elements: {
								rootBox: "w-full",
								card: "bg-transparent shadow-none",
								headerTitle: "text-[var(--neon-blue)] text-2xl font-bold",
								headerSubtitle: "text-[var(--gray-400)]",
								socialButtonsBlockButton: "bg-[var(--gray-700)] border-2 border-[var(--gray-600)] text-[var(--gray-100)] hover:border-[var(--neon-blue)] hover:shadow-[0_0_10px_rgba(0,240,255,0.5)] transition-all",
								socialButtonsBlockButtonText: "text-[var(--gray-100)] font-semibold",
								formFieldInput: "bg-[var(--gray-700)] border-2 border-[var(--gray-600)] text-[var(--gray-100)] focus:border-[var(--neon-blue)] focus:shadow-[0_0_10px_rgba(0,240,255,0.5)]",
								formButtonPrimary: "bg-[var(--neon-blue)] text-[var(--gray-900)] hover:bg-[var(--neon-blue-light)] shadow-[0_0_10px_rgba(0,240,255,0.5)] hover:shadow-[0_0_20px_rgba(0,240,255,0.6)] font-bold",
								footerActionLink: "text-[var(--neon-blue)] hover:text-[var(--neon-blue-light)]",
								identityPreviewText: "text-[var(--gray-100)]",
								identityPreviewEditButton: "text-[var(--neon-blue)]",
								formFieldLabel: "text-[var(--gray-300)]",
								dividerLine: "bg-[var(--gray-600)]",
								dividerText: "text-[var(--gray-400)]",
								formFieldInputShowPasswordButton: "text-[var(--neon-blue)]",
								otpCodeFieldInput: "bg-[var(--gray-700)] border-[var(--gray-600)] text-[var(--gray-100)]",
								alertText: "text-[var(--gray-100)]",
								formFieldErrorText: "text-[var(--accent-danger)]",
								formFieldSuccessText: "text-[var(--accent-success)]",
								formFieldWarningText: "text-[var(--accent-warning)]",
								footerActionText: "text-[var(--gray-400)]",
								footerAction: "justify-center mt-4"
							}
						}}
					/>
				</div>
				
				<div class="footer">
					<div class="footer-divider"></div>
					<p>Não tem uma conta? <a href="/cadastro">Crie uma agora</a></p>
					<div class="security-badge">
						<span class="badge-icon">🔒</span>
						<span>Conexão segura e criptografada</span>
					</div>
				</div>
			</div>
			
			<div class="decorative-elements">
				<div class="neon-circle neon-circle-1"></div>
				<div class="neon-circle neon-circle-2"></div>
				<div class="neon-line neon-line-1"></div>
				<div class="neon-line neon-line-2"></div>
			</div>
		</div>
	</main>
</Layout>

<style>
	main {
		position: relative;
		min-height: calc(100vh - 4rem);
		overflow: hidden;
	}

	.login-background {
		display: flex;
		align-items: center;
		justify-content: center;
		min-height: calc(100vh - 4rem);
		padding: 2rem;
		background: 
			radial-gradient(circle at 20% 30%, rgba(0, 240, 255, 0.15) 0%, transparent 50%),
			radial-gradient(circle at 80% 70%, rgba(0, 102, 255, 0.1) 0%, transparent 50%),
			var(--gray-900);
		position: relative;
	}

	.login-container {
		background: rgba(26, 26, 26, 0.95);
		backdrop-filter: blur(20px);
		padding: 3rem;
		border-radius: 1.5rem;
		border: 2px solid var(--neon-blue);
		box-shadow: 
			var(--shadow-neon-lg),
			0 0 80px rgba(0, 240, 255, 0.2),
			inset 0 0 60px rgba(0, 240, 255, 0.05);
		max-width: 480px;
		width: 100%;
		animation: fadeInScale 0.6s ease;
		position: relative;
		z-index: 10;
	}

	.login-container::before {
		content: '';
		position: absolute;
		top: -2px;
		left: -2px;
		right: -2px;
		bottom: -2px;
		background: linear-gradient(45deg, var(--neon-blue), var(--electric-blue), var(--neon-blue));
		border-radius: 1.5rem;
		z-index: -1;
		opacity: 0.3;
		filter: blur(10px);
		animation: rotate 4s linear infinite;
	}

	.header {
		text-align: center;
		margin-bottom: 2.5rem;
	}

	.logo-icon {
		font-size: 4rem;
		margin-bottom: 1rem;
		animation: float 3s ease-in-out infinite;
		filter: drop-shadow(0 0 20px var(--neon-blue));
	}

	.header h1 {
		color: var(--neon-blue);
		margin-bottom: 0.75rem;
		font-size: 2rem;
		font-weight: 800;
		text-shadow: var(--shadow-neon-md);
		background: linear-gradient(135deg, var(--neon-blue) 0%, var(--neon-blue-light) 100%);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
	}

	.header p {
		color: var(--gray-400);
		font-size: 1rem;
		line-height: 1.5;
	}

	.clerk-wrapper {
		margin-bottom: 2rem;
	}

	.footer {
		text-align: center;
		margin-top: 2rem;
	}

	.footer-divider {
		height: 2px;
		background: linear-gradient(90deg, transparent, var(--neon-blue), transparent);
		margin-bottom: 1.5rem;
		opacity: 0.3;
	}

	.footer p {
		color: var(--gray-400);
		font-size: 0.95rem;
		margin-bottom: 1.5rem;
	}

	.footer a {
		color: var(--neon-blue);
		text-decoration: none;
		font-weight: 700;
		transition: all 0.3s ease;
		position: relative;
		padding: 0.25rem 0.5rem;
		border-radius: 0.25rem;
	}

	.footer a::before {
		content: '';
		position: absolute;
		bottom: 0;
		left: 0;
		width: 0;
		height: 2px;
		background: var(--neon-blue);
		transition: width 0.3s ease;
	}

	.footer a:hover {
		color: var(--neon-blue-light);
		text-shadow: var(--shadow-neon-sm);
		background: rgba(0, 240, 255, 0.1);
	}

	.footer a:hover::before {
		width: 100%;
	}

	.security-badge {
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		padding: 0.75rem 1.5rem;
		background: rgba(0, 240, 255, 0.1);
		border: 1px solid var(--neon-blue);
		border-radius: 9999px;
		font-size: 0.85rem;
		color: var(--gray-300);
	}

	.badge-icon {
		font-size: 1.1rem;
	}

	/* Elementos Decorativos */
	.decorative-elements {
		position: absolute;
		inset: 0;
		pointer-events: none;
		overflow: hidden;
	}

	.neon-circle {
		position: absolute;
		border-radius: 50%;
		border: 2px solid var(--neon-blue);
		opacity: 0.2;
		animation: pulse 4s ease-in-out infinite;
	}

	.neon-circle-1 {
		width: 300px;
		height: 300px;
		top: -150px;
		left: -150px;
		box-shadow: 0 0 60px var(--neon-blue);
	}

	.neon-circle-2 {
		width: 400px;
		height: 400px;
		bottom: -200px;
		right: -200px;
		box-shadow: 0 0 80px var(--electric-blue);
		animation-delay: 2s;
	}

	.neon-line {
		position: absolute;
		height: 2px;
		background: linear-gradient(90deg, transparent, var(--neon-blue), transparent);
		opacity: 0.3;
		animation: slide 8s linear infinite;
	}

	.neon-line-1 {
		width: 400px;
		top: 20%;
		left: -400px;
	}

	.neon-line-2 {
		width: 500px;
		bottom: 30%;
		right: -500px;
		animation-delay: 4s;
	}

	@keyframes fadeInScale {
		from {
			opacity: 0;
			transform: scale(0.95) translateY(20px);
		}
		to {
			opacity: 1;
			transform: scale(1) translateY(0);
		}
	}

	@keyframes float {
		0%, 100% {
			transform: translateY(0);
		}
		50% {
			transform: translateY(-10px);
		}
	}

	@keyframes rotate {
		0% {
			transform: rotate(0deg);
		}
		100% {
			transform: rotate(360deg);
		}
	}

	@keyframes pulse {
		0%, 100% {
			transform: scale(1);
			opacity: 0.2;
		}
		50% {
			transform: scale(1.1);
			opacity: 0.4;
		}
	}

	@keyframes slide {
		0% {
			transform: translateX(0);
		}
		100% {
			transform: translateX(100vw);
		}
	}

	@media (max-width: 640px) {
		.login-container {
			padding: 2rem 1.5rem;
		}

		.header h1 {
			font-size: 1.75rem;
		}

		.logo-icon {
			font-size: 3rem;
		}

		.neon-circle-1,
		.neon-circle-2 {
			display: none;
		}
	}
</style>
