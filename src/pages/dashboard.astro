---
import { getUserRole, getDashboardPath } from '../lib/types';

// Redireciona para dashboard correto
export async function get({ Astro }) {
  // Em produção, buscar userId do contexto/middleware
  // Aqui assume que middleware já protege e redireciona
  return Astro.redirect('/');
}
---
// ...nenhum conteúdo HTML ou CSS...
